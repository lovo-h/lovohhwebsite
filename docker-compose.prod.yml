version: '2'

services:
    # =-=-=-=-= Ninx =-=-=-=-=
    nginx:
      build:
        context: .
        dockerfile: Dockerfile.prod
      container_name: LWNginx
      ports:
        - "80:80"
        - "443:443"
      restart: always
